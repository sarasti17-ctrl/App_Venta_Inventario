# Configuración de secretos para Streamlit
# IMPORTANTE: Este archivo contiene información sensible.
# NO lo subas a repositorios públicos.

[mysql]
host = "192.168.10.75"
port = 3306
database = "liquidacion_inventario"
user = "remoto"
password = "S@r@st1#.1990"

# Opcional: Configuración SSL para conexión segura
# ssl_ca = "/path/to/ca-cert.pem"
# ssl_cert = "/path/to/client-cert.pem"
# ssl_key = "/path/to/client-key.pem"

[app]
# Clave secreta para encriptación de sesiones (generada automáticamente)
secret_key = "liq-inv-2026-secure-key-a8f3b9c2d1e4f5a6"
